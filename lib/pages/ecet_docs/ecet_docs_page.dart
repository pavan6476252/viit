import 'package:flutter/material.dart';

class EcetDocsPage extends StatelessWidget {
  const EcetDocsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body:Center(child: Text("ecet page"),),);
  }
}